#TEMPLATE:
#INSERT IGNORE into TeamOverview (cpid)
#SELECT Project_user.cpid
#FROM Project_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT Asteroids_user.cpid
FROM Asteroids_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT ATLAS_user.cpid
FROM ATLAS_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT BitcoinUtopia_user.cpid
FROM BitcoinUtopia_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT BURP_user.cpid
FROM BURP_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT CSG_user.cpid
FROM CSG_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT Collatz_user.cpid
FROM Collatz_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT Cosmology_user.cpid
FROM Cosmology_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT DDM_user.cpid
FROM DDM_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT Einstein_user.cpid
FROM Einstein_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT Enigma_user.cpid
FROM Enigma_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT FindAtHome_user.cpid
FROM FindAtHome_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT GPUGRID_user.cpid
FROM GPUGRID_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT GridcoinFinance_user.cpid
FROM GridcoinFinance_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT LHCatHome_user.cpid
FROM LHCatHome_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT Leiden_user.cpid
FROM Leiden_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT MalariaControl_user.cpid
FROM MalariaControl_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT Milkyway_user.cpid
FROM Milkyway_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT MindModeling_user.cpid
FROM MindModeling_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT MooWrap_user.cpid
FROM MooWrap_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT NFS_user.cpid
FROM NFS_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT NumbersField_user.cpid
FROM NumbersField_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT POEM_user.cpid
FROM POEM_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT PrimeGrid_user.cpid
FROM PrimeGrid_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT Rosetta_user.cpid
FROM Rosetta_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT SAT_user.cpid
FROM SAT_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT SETI_user.cpid
FROM SETI_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT Skynet_user.cpid
FROM Skynet_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT Sztaki_user.cpid
FROM Sztaki_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT WCG_user.cpid
FROM WCG_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT Wuprop_user.cpid
FROM Wuprop_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT YAFU_user.cpid
FROM YAFU_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT YOYO_user.cpid
FROM YOYO_user;

INSERT IGNORE into TeamOverview (cpid)
SELECT vLHC_user.cpid
FROM vLHC_user;